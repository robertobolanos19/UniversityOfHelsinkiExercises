{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "lint": "eslint .",
    "test": "jest --verbose",
    "build:ui": "rm -rf build && cd ../exnotesfrontend/ && npm run build && cp -r build ../exnotesbackend",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.10.3"
  },
  "devDependencies": {
    "eslint": "^8.38.0",
    "lint": "^0.8.19",
    "nodemon": "^2.0.21"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "description": ""
}
